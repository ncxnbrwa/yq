package cn.iimedia.yq.Warning.adapter;

import android.support.v7.widget.RecyclerView;
import android.view.View;
import android.view.ViewGroup;
import android.widget.ImageView;
import android.widget.TextView;

import cn.iimedia.yq.R;

/**
 * Created by iiMedia on 2017/11/9.
 * 预警adapter
 */

public class WarningListAdapter extends RecyclerView.Adapter<RecyclerView.ViewHolder> {


    @Override
    public RecyclerView.ViewHolder onCreateViewHolder(ViewGroup parent, int viewType) {
        return null;
    }

    @Override
    public void onBindViewHolder(RecyclerView.ViewHolder holder, int position) {

    }

    @Override
    public int getItemCount() {
        return 0;
    }

    class WarningViewHolder extends RecyclerView.ViewHolder {
        ImageView ivWarning;
        TextView tvColor;
        TextView tvDetail;
//        TextView tvTime;
//        TextView tvCompany;

        public WarningViewHolder(View itemView) {
            super(itemView);
            ivWarning = (ImageView) itemView.findViewById(R.id.iv_warning);
            tvColor = (TextView) itemView.findViewById(R.id.tv_warning_color);
            tvDetail = (TextView) itemView.findViewById(R.id.tv_warning_detail);
//            tvTime = (TextView) itemView.findViewById(R.id.tv_warning_time);
//            tvCompany = (TextView) itemView.findViewById(R.id.tv_warning_company);
        }
    }
}
